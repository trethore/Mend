@@ -1,5 +1,6 @@
 interface User {
     id: number;
     name: string;
+    email?: string;
 }
 
-function processUser(user: User) {
-    console.log("Processing " + user.name);
+function processUser( user: User ) {
+    console.log(`Processing ${user.name}`);
 }
